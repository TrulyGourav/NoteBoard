# Server port
server.port=8080

# Database connection settings
spring.datasource.url=jdbc:postgresql://noteboard.cptgoqe3dawn.us-east-1.rds.amazonaws.com:5432/noteboardMS
spring.datasource.username=noteboard
spring.datasource.password=noteboard

# Hibernate dialect for PostgreSQL
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQL95Dialect

# Hibernate automatic table creation (optional)
spring.jpa.hibernate.ddl-auto=update

# Logging settings (optional)
spring.jpa.show-sql=true
#spring.jpa.properties.hibernate.format_sql=true

